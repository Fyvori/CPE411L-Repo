<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Activity 3 Simple Arithmetic Calculator</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .calculator {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background: white;
            border-radius: 8px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        .result {
            font-size: 1.5rem;
            font-weight: bold;
            margin-top: 20px;
        }
        .operator-select {
            width: 110px; 
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2 class="text-center my-4">Simple Arithmetic Calculator</h2>
        <div class="calculator p-3">
            <form action="/calculator" method="POST" class="d-flex">
                <input type="number" name="num1" class="form-control me-2" required value="<%= num1 !== null ? num1 : '' %>">
                <select name="operator" class="form-select me-2 operator-select">
                    <option value="+" <%= operator === '+' ? 'selected' : '' %>>+</option>
                    <option value="-" <%= operator === '-' ? 'selected' : '' %>>-</option>
                    <option value="*" <%= operator === '*' ? 'selected' : '' %>>×</option>
                    <option value="/" <%= operator === '/' ? 'selected' : '' %>>÷</option>
                    <option value="%" <%= operator === '%' ? 'selected' : '' %>>%</option>
                    <option value="&&" <%= operator === '&&' ? 'selected' : '' %>>&</option>
                    <option value="||" <%= operator === '||' ? 'selected' : '' %>>||</option>
                    <option value="!" <%= operator === '!' ? 'selected' : '' %>>!</option>
                    <option value=">" <%= operator === '>' ? 'selected' : '' %>>></option>
                    <option value="<" <%= operator === '<' ? 'selected' : '' %>><</option>
                    <option value="==" <%= operator === '==' ? 'selected' : '' %>>==</option>
                    <option value="===" <%= operator === '===' ? 'selected' : '' %>>===</option>
                    <option value="!=" <%= operator === '!=' ? 'selected' : '' %>>!=</option>
                    <option value="even/odd" <%= operator === 'even/odd' ? 'selected' : '' %>>Odd/Even</option>
                </select>
                <input type="number" name="num2" class="form-control me-2" required value="<%= num2 !== null ? num2 : '' %>">
                <button type="submit" class="btn btn-success">Calculate</button>
            </form>
            
            <% if (result !== null) { %>
                <div class="mt-4 text-center">
                    <p class="result">Result: <%= result %></p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
